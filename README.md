# Python Practice

Core Python exercises, kept around as a reference for myself more than anything else.

- `Python Basics/` — data types, operators, slicing, OOP/inheritance basics, reading and writing files.
- `Python Questions/` — small standalone problems (`Q1.py`, `Q2.py`, ...), each one self-contained.

Nothing here is meant to be run as a package — just open a file and run it.
